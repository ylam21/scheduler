# scheduler
table-wizard recall

config users in:
 - users.c

set days in month in file:
 - setup.h

```
git clone git@github.com:ylam21/scheduler.git scheduler
```
```
cd/scheduler
```
```
make && ./a.out
```
